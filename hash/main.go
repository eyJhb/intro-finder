package hash

func main() {
	// filename := "out0020.jpeg"
	// infile, err := os.Open(filename)
	// if err != nil {
	// 	panic(err.Error())
	// }
	// defer infile.Close()

	// // Must specifically use jpeg.Decode() or it
	// // would encounter unknown format error
	// src, err := jpeg.Decode(infile)
	// if err != nil {
	// 	panic(err.Error())
	// }

	// aHash(src)

	// fmt.Println("vim-go")
}
